spring:
  application:
    name: enterprise-loan-management-system
  datasource:
    url: jdbc:postgresql://localhost:5432/loan_management
    username: pallavinemade
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
